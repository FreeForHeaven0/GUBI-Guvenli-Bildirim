services:
  - type: web
    name: gubi-api
    runtime: node
    buildCommand: npm install
    startCommand: npx json-server --watch db.json --port 10000 --host 0.0.0.0
    envVars:
      - key: NODE_VERSION
        value: 18
